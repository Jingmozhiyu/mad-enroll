# 手动管理学期

[English](../en-us/term-management.md) · [API 契约](api-reference.md)

## 行为

| 状态 | 搜索和添加 / 重新启用 | 定时轮询和课程提醒 | 状态变更对订阅的影响 |
| --- | --- | --- | --- |
| UPCOMING | 允许 | 暂停 | 无 |
| ACTIVE | 允许 | 仅已启用的订阅 | 无 |
| EXPIRED | 拒绝 | 暂停 | 禁用该学期中所有已启用的订阅 |

禁用自己拥有的订阅始终允许，包括学期过期后重复发起请求。未知的学期代码会被拒绝。多个学期可以同时处于 ACTIVE 状态。重新开放 EXPIRED 学期不会重新启用订阅。每个用户启用的 section 数量仍有 15 个的现有限制，该限制仍计入 UPCOMING 订阅。

课程和 section 记录继续保存在共享表中。section 的学期来自其课程。课程查询只能使用 `(termCode, courseId)`。

## 首次部署

1. 备份数据库，并在部署此后端**之前**针对现有 MySQL 数据库执行 `backend/migrations/20260907_terms.sql`。仓库使用 Hibernate schema 更新，而不是自动迁移运行器；这条 SQL 是明确的部署步骤。
2. 脚本会创建 `terms`，并将现有课程的学期代码导入为 ACTIVE，以保留现有监控。重复运行时不会覆盖状态。检查导入的列表；部署后通过管理 API/UI 将过时的学期设为过期。解决最终查询报告的所有无效代码。如果数据库中没有课程，请通过管理 UI 创建所需学期，并在准备好后明确将其激活。
3. 部署后端，然后部署前端。在 `/admin` 中使用 **Terms**，将学期添加为 UPCOMING、激活监控、将其暂停为 UPCOMING，或将其设为过期。在前端公开任何尚无课程记录但可搜索的学期之前，先为其完成配置。
4. 前端搜索标签和学期代码环境变量仍由人工管理。它们不能授权后端拒绝的学期。不会进行定时学期状态转换，也不会自动初始化改变服务状态。

## 管理 API

所有端点都要求经过身份验证的 ADMIN，身份验证由 `AdminService` 完成。响应使用现有的 `{code, msg, data}` 封装。

- `GET /api/admin/terms`：返回 `[{code, label, status}]`，按代码从新到旧排列。
- `POST /api/admin/terms`，请求体 `{"code":"1272","label":"Fall 2026"}`：创建一个 UPCOMING 学期。代码必须为四位数字，标签长度为 1–80 个字符，重复代码会被拒绝。
- `PATCH /api/admin/terms/{termCode}`，请求体 `{"status":"EXPIRED"}`：返回 `{"term":{"code":"1272","label":"Fall 2026","status":"EXPIRED"},"disabledSubscriptions":12}`。ACTIVE / UPCOMING 不会修改订阅。EXPIRED 会在一个事务中更新学期并禁用其订阅。重复执行过期操作也会成功，新禁用的行数为零。

## 并发与排队工作

添加或启用订阅时，会对 `terms` 主键行获取共享悲观读锁；变更学期状态时，会对同一行获取排他悲观写锁。两种锁都会一直持有到外层服务事务结束。在 MySQL 中，订阅事务可以共享学期锁，而状态变更会排除订阅事务。同一课程或订阅上的写操作仍可能互相竞争。持有此锁期间不会发起爬虫请求或 SMTP 发送。

先完成的添加操作会被纳入之后的批量禁用。如果过期操作先获取锁，等待中的添加操作会读取到 EXPIRED 并失败。管理端启用订阅使用相同的保护机制。学期代码不可编辑，因此订阅在等待期间不能切换学期。

锁定学期后，每个用户/管理端启用操作都会以排他方式锁定订阅所有者的 `users` 行。随后，它会在该事务中加载订阅状态、统计所有学期的已启用订阅，并创建/重新启用订阅。这些入口使用 READ_COMMITTED，因此等待锁后读取到已提交的变更。管理端代码最初只读取所有者和学期的标量标识符，避免使用锁前缓存的订阅实体。这可以防止两个并发请求同时占用第 15 个名额。禁用只会减少计数，不需要此容量保护；管理端禁用不会永久阻止用户重新启用。

Heartbeat 查询只选择 ACTIVE 学期中、拥有已启用订阅且已到期的课程。出队检查会在现有订阅查询中包含 ACTIVE；不会额外查询学期。邮件消费会在一次查询中检查已启用订阅和 ACTIVE 学期。手动测试邮件、欢迎邮件和反馈不受学期监控影响。没有订阅 ID 的非测试课程提醒会被丢弃。

正在执行的爬取可能在状态变更后完成；邮件消费者会过滤后续课程提醒。已经通过最后一次检查或已到达 SMTP 的发送无法撤回。

## 验证

`TermLifecycleTest` 使用 H2 针对真实的 Spring 服务事务和仓库进行测试，涵盖同时添加/过期操作的两种先后顺序、回滚、多个 ACTIVE 学期、UPCOMING 订阅选择、未知/已过期学期拒绝、排队工作过滤，以及在第三个学期同步同一课程。邮件和爬虫集成均使用 mock。跨学期并发测试在 14 个订阅时覆盖用户/用户、用户/管理端和管理端/管理端启用；不同的学期行确保 H2 更强的学期锁不会掩盖缺失的用户锁。H2 测试明确使用 H2 方言，而不是继承生产环境的 MySQL 方言。H2 会对悲观读使用排他锁，因此这些测试验证添加/过期互斥，但不能证明 MySQL 上订阅事务可以并行执行。生产仓库使用标准 JPA 锁注解，不包含数据库特定的 adapter 代码。

使用 Java 21 运行后端测试。如果本地无法使用 Mockito 自附加，请通过 Surefire 的 `-DargLine=-javaagent:/absolute/path/to/mockito-core.jar` 使用匹配的 Mockito jar。
